# Reproducibility Levels on Nondeterministic Hardware

> _Stub — full content lands in a later phase._

Bit-exact reruns are largely unachievable on real GPU hardware; statistical/pipeline reproducibility — pinned code, config, data, and container — is the realistic target.
