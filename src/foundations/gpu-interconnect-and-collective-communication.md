# GPU Interconnect & Collective Communication

> _Stub — full content lands in a later phase._

Cross-node fabric speed sets the ceiling on distributed training scale-out, since collective operations like all-reduce cannot outrun the slowest link.
