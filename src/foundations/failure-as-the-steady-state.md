# Failure as the Steady State at Fleet Scale

> _Stub — full content lands in a later phase._

At thousands of GPUs, component failure during a long training run is a certainty, not an edge case, and platform design has to start from that assumption.
