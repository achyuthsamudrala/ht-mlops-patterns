# Consensus & the CAP Tradeoff for Cluster State

> _Stub — full content lands in a later phase._

Kubernetes' control plane deliberately trades availability for consistency during a partition, and that choice shapes every cluster-scale failure mode.
